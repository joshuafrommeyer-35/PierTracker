# Extra packages for the one-time setup_models.py (download + convert the models).
# Install CPU-only PyTorch first so nothing pulls a multi-GB CUDA build:
#   pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cpu
-r requirements.txt
rfdetr==1.11.0
open_clip_torch==3.3.0
onnx==1.23.0
